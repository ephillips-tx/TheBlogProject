namespace TheBlogProject.Enums
{
    public enum ReadyStatus // change class to enum: generally used to avoid user / coder type a string
    {
        Incomplete,
        ProductionReady,
        PreviewReady
    }
}
